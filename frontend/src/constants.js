// never store JWT Tokens in local storage or session data instead we store it in http only cookie or In-Memory Storage (plus securisé).
export const ACCESS_TOKEN = "access";
export const REFRESH_TOKEN = "refresh";
